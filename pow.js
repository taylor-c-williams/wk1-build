// In this build you will use the Red, Green, Refactor approach to create a function that takes a base and exponent and raises the base to the exponent power:
// base<sup>exponent</sup>.
const pow = (base, exponent) => {
const solution = Math.pow(base, exponent)
return solution
};
module.exports = pow;
  